title: Premier cours de Python
plan:
- title: Cours - Variables et opérations essai2 encore et encore!!!
  statement: Variables_et_fonctions/Cours variables et operations.md
projects:
  python:
    buildCommand: pip install numpy matplotlib Pillow; pip install scipy numba 
    #buildCommand: echo 'export PYTHONPATH=/project/target/modules' >> /root/.profile; echo 'export PYTHONPATH=$PYTHONPATH:/project/target/modules' >> /root/.bashrc;. ~/.bashrc
    #buildCommand: cp -r /project/target/modules/* /usr/local/lib/python3.7/site-packages #Pour copier directement les modules au lieu de les importer mais problemes de versions...
    root: /python-project
    runner:
      name: python
      version: '3.6'